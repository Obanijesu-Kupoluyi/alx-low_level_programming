#include <stdio.h>
#include "main.h"
/**
 * add - Entry point
 * @x: entered value
 * @y: entered value
 * Return: result
 */
int add(int x, int y)
{
	int result;

	result = x + y;

	return (result);
}
