#include <stdio.h>
/**
 * main- Serves as the entry point fot the program
 * Description- prints \"Programming is like building a miltilingual puzzle
 * Return: Returns zero if no errors otherwise returns non-zero value
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
