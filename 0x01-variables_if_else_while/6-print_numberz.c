#include <stdlib.h>
#include <stdio.h>
/**
 * main- Serves as the entry of the program
 * Return: zero
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(n + '0');
	putchar('\n');
	return (0);
}
