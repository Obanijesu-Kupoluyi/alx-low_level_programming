#include <stdlib.h>
#include <stdio.h>
/**
 * main- Serves as the main entry of the program
 * Return: return is zero, non-zero value if any error
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d\n", n);
	return (0);
}
